[project]
name = "requiem_testing"
version = "0.4.3"
authors = [
    {name = "Polllaris"}
]
dependencies = [
    "pytest==8.3.5"
]
description = "Tools for testing & distribution on the requiem world project."

[project.optional-dependencies]
experimental = [
    "sanic"
]